name: Hello World

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkour@v2
      - name: hello world
        run: echo "hello world"
        shell: bash
